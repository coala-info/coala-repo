---
name: bioconductor-mmdiff2
description: MMDiff2 detects statistically significant differences between ChIP-seq peak profiles by comparing their shapes and intensities. Use when user asks to compare ChIP-seq datasets, identify differential peak shapes, or analyze differences in genomic signal profiles.
homepage: https://bioconductor.org/packages/release/bioc/html/MMDiff2.html
---


# bioconductor-mmdiff2

## Overview

Use the Bioconductor R package **MMDiff2** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("MMDiff2")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.