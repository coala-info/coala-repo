---
name: bioconductor-clustcomp
description: ClustComp compares and visualizes the relationships between different clusterings of the same dataset. Use when user asks to compare clustering results, visualize overlaps between different partitions, or analyze the relationship between multiple clustering solutions.
homepage: https://bioconductor.org/packages/release/bioc/html/clustComp.html
---


# bioconductor-clustcomp

## Overview

Use the Bioconductor R package **clustComp** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("clustComp")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.