---
name: bioconductor-scfeaturefilter
description: scFeatureFilter filters features in single-cell RNA-seq data by analyzing expression patterns across multiple groups. Use when user asks to filter genes in single-cell datasets, identify features with specific expression profiles, or preprocess single-cell transcriptomics data.
homepage: https://bioconductor.org/packages/release/bioc/html/scFeatureFilter.html
---


# bioconductor-scfeaturefilter

## Overview

Use the Bioconductor R package **scFeatureFilter** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("scFeatureFilter")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.