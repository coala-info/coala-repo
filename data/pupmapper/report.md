# pupmapper CWL Generation Report

## pupmapper_all

### Tool Description
Directory for all outputs of k-mer and pileup mappability processing.

### Metadata
- **Docker Image**: quay.io/biocontainers/pupmapper:0.2.0--pyhdfd78af_0
- **Homepage**: https://github.com/maxgmarin/pupmapper
- **Package**: https://anaconda.org/channels/bioconda/packages/pupmapper/overview
- **Validation**: PASS

- **Conda**: https://anaconda.org/channels/bioconda/packages/pupmapper/overview
- **Total Downloads**: 382
- **Last updated**: 2026-02-09
- **GitHub**: https://github.com/maxgmarin/pupmapper
- **Stars**: N/A
### Original Help Text
```text
usage: pupmapper all [-h] -i IN_GENOME_FA -o OUTDIR -k KMER_LEN -e ERRORS
                     [-g GFF] [-p PREFIX] [--save-numpy] [--save-bigwig]
                     [--verbose]

options:
  -h, --help            show this help message and exit
  -i IN_GENOME_FA, --in_genome_fa IN_GENOME_FA
                        Input genome fasta file (.fasta)
  -o OUTDIR, --outdir OUTDIR
                        Directory for all outputs of k-mer and pileup
                        mappability processing.
  -k KMER_LEN, --kmer_len KMER_LEN
                        k-mer length (bp) used to generate the k-mer
                        mappability values
  -e ERRORS, --errors ERRORS
                        Number of allowed mismatches (hamming distance) in
                        k-mer mapping step.
  -g GFF, --gff GFF     GFF formatted genome annotations for input genome
                        (.gff) (Optional)
  -p PREFIX, --prefix PREFIX
                        File prefix for all output files generated by
                        Pupmapper. By default, the prefix is the basename of
                        the input genome fasta file.
  --save-numpy          If enabled, all pileup mappability scores will be
                        output as compressed numpy arrays (.npz).
  --save-bigwig         If enabled, pileup mappability scores will be output
                        in .bigwig format.
  --verbose             Output additional messages during processing. Useful
                        for troubleshooting.
```


## pupmapper_genmap

### Tool Description
Generate k-mer mappability values using Genmap (indexing and mapping steps)

### Metadata
- **Docker Image**: quay.io/biocontainers/pupmapper:0.2.0--pyhdfd78af_0
- **Homepage**: https://github.com/maxgmarin/pupmapper
- **Package**: https://anaconda.org/channels/bioconda/packages/pupmapper/overview
- **Validation**: PASS

### Original Help Text
```text
usage: pupmapper genmap [-h] -i IN_GENOME_FA -o OUTDIR -k KMER_LEN -e ERRORS
                        [--verbose]

options:
  -h, --help            show this help message and exit
  -i IN_GENOME_FA, --in_genome_fa IN_GENOME_FA
                        Input genome fasta file (.fasta)
  -o OUTDIR, --outdir OUTDIR
                        Directory for all outputs of k-mer mappability
                        processing w/ Genmap (indexing and mapping steps)
  -k KMER_LEN, --kmer_len KMER_LEN
                        k-mer length (bp) used to generate the k-mer
                        mappability values
  -e ERRORS, --errors ERRORS
                        Number of allowed mismatches (hamming distance) in
                        k-mer mapping step.
  --verbose             Output additional messages during processing. Useful
                        for troubleshooting.
```


## Metadata
- **Skill**: generated
