---
name: bioconductor-rolde
description: RolDE identifies differentially expressed genes in longitudinal multi-group transcriptomics data. Use when user asks to detect differential expression in time-course experiments, analyze longitudinal data with multiple groups, or identify genes with varying expression patterns over time.
homepage: https://bioconductor.org/packages/release/bioc/html/RolDE.html
---


# bioconductor-rolde

## Overview

Use the Bioconductor R package **RolDE** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("RolDE")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.