# neat CWL Generation Report

## neat_gen-mut-model

### Tool Description
Generate mutation model from a pickle or BED file and user input.

### Metadata
- **Docker Image**: quay.io/biocontainers/neat:4.3.5--pyhdfd78af_0
- **Homepage**: https://github.com/ncsa/NEAT/
- **Package**: https://anaconda.org/channels/bioconda/packages/neat/overview
- **Validation**: PASS

- **Conda**: https://anaconda.org/channels/bioconda/packages/neat/overview
- **Total Downloads**: 6.1K
- **Last updated**: 2025-11-21
- **GitHub**: https://github.com/ncsa/NEAT
- **Stars**: N/A
### Original Help Text
```text
usage: neat gen-mut-model [-h] [-b BED] [--outcounts OUTCOUNTS]
                          [--show-trinuc] [--human_sample] [--skip_common]
                          [--save-trinuc] [--overwrite] [-o OUTPUT_DIR]
                          [-p PREFIX]
                          reference.fa mutation.vcf

Generate mutation model from a pickle or BED file and user input.

positional arguments:
  reference.fa          Reference file for organism in fasta format
  mutation.vcf          Mutation file for organism in VCF format

options:
  -h, --help            show this help message and exit
  -b BED, --bed BED     Bed file with regions to use in the model
  --outcounts OUTCOUNTS
                        Path to trinucleotide counts file for reference
                        (generated by a previous run).If the input file in
                        empty, then instead the output of this run will be
                        saved there. Note, this file will not be used if a bed
                        file is also used.
  --show-trinuc         Shows trinucleotide counts, for reference
  --human_sample        Only use numbered chroms, X, Y, and MT. Omit this flag
                        to include all chroms in reference.
  --skip_common         Includes a list of common variants, if you want to
                        visualize common variants with plot_mut_model.py.
  --save-trinuc         Saves trinucleotide counts to a separate file
  --overwrite           Set this flag to overwrite existing output.

output:
  -o OUTPUT_DIR, --output_dir OUTPUT_DIR
                        Path to the output directory. Will create if not
                        present.
  -p PREFIX, --prefix PREFIX
                        Prefix to use to name files
```


## neat_model-fraglen

### Tool Description
Generate fragment length model from a BAM or SAM file_list.

### Metadata
- **Docker Image**: quay.io/biocontainers/neat:4.3.5--pyhdfd78af_0
- **Homepage**: https://github.com/ncsa/NEAT/
- **Package**: https://anaconda.org/channels/bioconda/packages/neat/overview
- **Validation**: PASS

### Original Help Text
```text
usage: neat model-fraglen [-h] -i input_file
                          [--min_reads minimum_number_reads]
                          [--overwrite overwrite_output] [-o OUTPUT_DIR]
                          [-p PREFIX]

Generate fragment length model from a BAM or SAM file_list.

options:
  -h, --help            show this help message and exit
  -i input_file         Bam or sam input file_list.
  --min_reads minimum_number_reads
                        Minimum number of reads for a fragment length to
                        consider it in the model. The default is 2, to handle
                        smaller datasets. Set to 0 to turn off filtering. For
                        a larger dataset, try 100 and adjust from there.
  --overwrite overwrite_output
                        Set this flag to overwrite existing output.

output:
  -o OUTPUT_DIR, --output_dir OUTPUT_DIR
                        Path to the output directory. Will create if not
                        present.
  -p PREFIX, --prefix PREFIX
                        Prefix to use to name files
```


## neat_model-seq-err

### Tool Description
Generate sequencing error model from a FASTQ, BAM, or SAM file_list.

### Metadata
- **Docker Image**: quay.io/biocontainers/neat:4.3.5--pyhdfd78af_0
- **Homepage**: https://github.com/ncsa/NEAT/
- **Package**: https://anaconda.org/channels/bioconda/packages/neat/overview
- **Validation**: PASS

### Original Help Text
```text
usage: neat model-seq-err [-h] -i FILE [FILE ...] [-q OFFSET]
                          [-Q QUAL_SCORE [QUAL_SCORE ...]] [-m MAX]
                          [--overwrite] [-o OUTPUT_DIR] [-p PREFIX]

Generate sequencing error model from a FASTQ, BAM, or SAM file_list.

options:
  -h, --help            show this help message and exit
  -i FILE [FILE ...]    fastq(.gz) to process. Entering 1 fastqs will set the
                        modeler to single-ended mode. Use -i2 for paired ended
                        models.To use a sam/bam input first convert to fastq
                        using an external tool, such as samtools.
  -q OFFSET             quality score offset [33]
  -Q QUAL_SCORE [QUAL_SCORE ...]
                        Quality score max. The default 42. The lowest possible
                        score is 1. To used binnedscoring, enter a space
                        separated list of scores, e.g., -Q 2 15 23 37
  -m MAX                Max number of reads to process [default: all].
  --overwrite           Overwrite previous output files. Default is to throw
                        an error if the file_list already exists.

output:
  -o OUTPUT_DIR, --output_dir OUTPUT_DIR
                        Path to the output directory. Will create if not
                        present.
  -p PREFIX, --prefix PREFIX
                        Prefix to use to name files
```


## neat_read-simulator

### Tool Description
NEAT read simulator

### Metadata
- **Docker Image**: quay.io/biocontainers/neat:4.3.5--pyhdfd78af_0
- **Homepage**: https://github.com/ncsa/NEAT/
- **Package**: https://anaconda.org/channels/bioconda/packages/neat/overview
- **Validation**: PASS

### Original Help Text
```text
NEAT run log: /1772129537.6664088_NEAT.log
2026-02-26 18:12:21,148:INFO:neat.common.logging:writing log to: /1772129537.6664088_NEAT.log
2026-02-26 18:12:21,149:INFO:neat.read_simulator.runner:Using configuration file None
2026-02-26 18:12:21,149:ERROR:neat:read-simulator failed, see the traceback below
Traceback (most recent call last):
  File "/usr/local/lib/python3.11/site-packages/neat/cli/cli.py", line 133, in main
    cmd(args)
  File "/usr/local/lib/python3.11/site-packages/neat/cli/commands/read_simulator.py", line 48, in execute
    read_simulator_runner(arguments.config, arguments.output_dir, arguments.prefix)
  File "/usr/local/lib/python3.11/site-packages/neat/read_simulator/runner.py", line 55, in read_simulator_runner
    config = Path(config).resolve()
             ^^^^^^^^^^^^
  File "/usr/local/lib/python3.11/pathlib.py", line 871, in __new__
    self = cls._from_parts(args)
           ^^^^^^^^^^^^^^^^^^^^^
  File "/usr/local/lib/python3.11/pathlib.py", line 509, in _from_parts
    drv, root, parts = self._parse_args(args)
                       ^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/local/lib/python3.11/pathlib.py", line 493, in _parse_args
    a = os.fspath(a)
        ^^^^^^^^^^^^
TypeError: expected str, bytes or os.PathLike object, not NoneType
ERROR: read-simulator failed, showing the last error
```

