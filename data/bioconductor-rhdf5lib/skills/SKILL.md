---
name: bioconductor-rhdf5lib
description: Rhdf5lib provides the HDF5 C library for use within the R ecosystem. Use when user asks to access HDF5 C libraries, compile R packages that depend on HDF5, or interface with HDF5 data structures.
homepage: https://bioconductor.org/packages/release/bioc/html/Rhdf5lib.html
---


# bioconductor-rhdf5lib

## Overview

Use the Bioconductor R package **Rhdf5lib** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("Rhdf5lib")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.