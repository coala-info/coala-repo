---
name: bioconductor-tadcompare
description: TADCompare identifies and characterizes differential Topologically Associating Domains (TADs) between multiple Hi-C contact maps. Use when user asks to compare TAD boundaries across conditions, identify stable or shifted TADs, or perform differential analysis of chromatin domains.
homepage: https://bioconductor.org/packages/release/bioc/html/TADCompare.html
---


# bioconductor-tadcompare

## Overview

Use the Bioconductor R package **TADCompare** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("TADCompare")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.