---
name: bioconductor-uncoverapplib
description: uncoverappLib provides an interactive tool for analyzing and visualizing sequence coverage in clinical NGS data. Use when user asks to visualize sequence coverage, identify low-coverage regions, or evaluate the clinical utility of sequencing data.
homepage: https://bioconductor.org/packages/release/bioc/html/uncoverappLib.html
---


# bioconductor-uncoverapplib

## Overview

Use the Bioconductor R package **uncoverappLib** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("uncoverappLib")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.