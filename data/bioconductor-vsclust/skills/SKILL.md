---
name: bioconductor-vsclust
description: Bioconductor-vsclust performs variance-stabilized clustering of multi-condition omics data to identify feature patterns. Use when user asks to cluster gene expression data, perform variance-stabilized clustering, or analyze multi-condition omics datasets.
homepage: https://bioconductor.org/packages/release/bioc/html/vsclust.html
---


# bioconductor-vsclust

## Overview

Use the Bioconductor R package **vsclust** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("vsclust")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.