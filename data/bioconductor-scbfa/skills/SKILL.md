---
name: bioconductor-scbfa
description: This tool performs dimensionality reduction for single-cell data using a binary factor analysis framework. Use when user asks to reduce the dimensionality of single-cell datasets, model binary feature detection, or identify latent factors in sparse genomic data.
homepage: https://bioconductor.org/packages/release/bioc/html/scBFA.html
---


# bioconductor-scbfa

## Overview

Use the Bioconductor R package **scBFA** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("scBFA")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.