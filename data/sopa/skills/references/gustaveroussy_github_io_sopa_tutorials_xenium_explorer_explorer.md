Xenium Explorer usage - Sopa Skip to content Sopa Xenium Explorer usage Initializing search gustaveroussy/sopa Sopa gustaveroussy/sopa Home Getting Started Tutorials Tutorials API usage Snakemake pipeline Tech-specific advice Xenium Explorer usage Xenium Explorer usage Table of contents Explorer directory creation Update the cell categories/clusters Option 1: via the AnnData object Option 2: via the SpatialData object Visualize the new categories in the explorer Filtering cells Use the coordinates of a lasso selection in SpatialData Selecting cells from a selection Cropping a SpatialData object from a selection Segmentation overlay Landmark-based alignment Comparing segmentations Spatial operations Visium HD tutorial H&amp;E usage Command line interface Other segmentations Other segmentations ComSeg usage Multi-step Custom segmentation API API Readers Patches Segmentation Aggregation Spatial operations Utils Misc Frequently asked questions Cite us Table of contents Explorer directory creation Update the cell categories/clusters Option 1: via the AnnData object Option 2: via the SpatialData object Visualize the new categories in the explorer Filtering cells Use the coordinates of a lasso selection in SpatialData Selecting cells from a selection Cropping a SpatialData object from a selection Segmentation overlay Xenium Explorer usage This tutorial shows interoperability tools between Sopa and the Xenium Explorer. We show how to go back and forth, between analysis and visualization. Ensure that you have already run Sopa, either with the Snakemake pipeline, CLI, or API. For image alignment with the Xenium Explorer, refer to this tutorial . Xenium Explorer is a registered trademark of 10x Genomics. The Xenium Explorer is licensed for usage on Xenium data (more details here ). In [ ]: Copied! import sopa import sopa Explorer directory creation ¶ For this tutorial, we use some generated data that looks similar to the output of Sopa. In [ ]: Copied! # if you have your own data, then # use `sdata = spatialdata.read_zarr("...")` instead sdata = sopa . io . toy_dataset ( as_output = True ) # if you have your own data, then # use `sdata = spatialdata.read_zarr("...")` instead sdata = sopa.io.toy_dataset(as_output=True) In [ ]: Copied! # directory where we'll save all the Xenium Explorer files explorer_path = "tuto.explorer" # directory where we'll save all the Xenium Explorer files explorer_path = "tuto.explorer" Now, we need to create the Xenium Explorer input files. To do so, you can use sopa.io.explorer.write as below: If you have already run Sopa on your own data, you have likely already done the step below. In [ ]: Copied! sopa . io . explorer . write ( explorer_path , sdata ) sopa.io.explorer.write(explorer_path, sdata) [INFO] (sopa.io.explorer.table) Writing table with 6 columns [INFO] (sopa.io.explorer.table) Writing 2 cell categories: region, slide [INFO] (sopa.io.explorer.shapes) Writing 400 cell polygons [INFO] (sopa.io.explorer.points) Writing 40000 transcripts [INFO] (sopa.io.explorer.points) &gt; Level 0: 40000 transcripts [INFO] (sopa.io.explorer.points) &gt; Level 1: 10000 transcripts [INFO] (sopa.io.explorer.images) Writing multiscale image with procedure=semi-lazy (load in memory when possible) [INFO] (sopa.io.explorer.images) (Loading image of shape (4, 2048, 2048)) in memory [INFO] (sopa.io.explorer.images) &gt; Image of shape (4, 2048, 2048) [INFO] (sopa.io.explorer.images) &gt; Image of shape (4, 1024, 1024) [INFO] (sopa.io.explorer.images) &gt; Image of shape (4, 512, 512) [INFO] (sopa.io.explorer.images) &gt; Image of shape (4, 256, 256) [INFO] (sopa.io.explorer.images) &gt; Image of shape (4, 128, 128) [INFO] (sopa.io.explorer.images) &gt; Image of shape (4, 64, 64) [INFO] (sopa.io.explorer.converter) Saved files in the following directory: tuto.explorer [INFO] (sopa.io.explorer.converter) You can open the experiment with 'open tuto.explorer/experiment.xenium' Now, inside the explorer_path directory, you have an experiment.xenium file that you can open with the Xenium Explorer . Update the cell categories/clusters ¶ Here, we run some Leiden clustering with scanpy . Then, we will update the Xenium Explorer files to display the spot clusters. More generally, you can add new cell categories, i.e. a column of sdata["table"].obs , and the Xenium Explorer will show it after the instructions below. Note that we only display categorical columns. If a column from sdata["table"].obs contains continuous numerical values (e.g., 3.13, 7.89, ... ), it will not be transformed into a categorical variable, and therefore not shown in the Xenium Explorer. In this case, we recommend using the spatiadata_plot static plotting library or the napari_spatialdata interactive plotting library. In [ ]: Copied! import scanpy as sc # for convenience, we extract the AnnData table as a new variable adata = sdata . tables [ "table" ] import scanpy as sc # for convenience, we extract the AnnData table as a new variable adata = sdata.tables["table"] In [ ]: Copied! sc . pp . normalize_total ( adata ) sc . pp . log1p ( adata ) sc . pp . pca ( adata ) sc . pp . neighbors ( adata ) sc . tl . umap ( adata ) sc . tl . leiden ( adata , resolution = 0.1 ) sc.pp.normalize_total(adata) sc.pp.log1p(adata) sc.pp.pca(adata) sc.pp.neighbors(adata) sc.tl.umap(adata) sc.tl.leiden(adata, resolution=0.1) In [7]: Copied! sc . pl . umap ( adata , color = "leiden" ) sc.pl.umap(adata, color="leiden") Option 1: via the AnnData object ¶ Now, you can update the explorer with your new cluster assignment. You don't need to re-run the complete conversion; you can edit the analysis.zarr.zip file only, as below. Note that the second argument of write_cell_categories is the AnnData object corresponding to sdata["table"] . In [8]: Copied! sopa . io . explorer . write_cell_categories ( explorer_path , adata ) sopa.io.explorer.write_cell_categories(explorer_path, adata) [INFO] (sopa.io.explorer.table) Writing 3 cell categories: region, slide, leiden Option 2: via the SpatialData object ¶ You can also re-use sopa.io.explorer.write with the SpatialData object, but provide mode="+o" to update only the o bservation file (i.e., the explorer file containing the information in sdata["table"].obs ). Note that, it assumes the cells boundaries didn't change since you last ran sopa.io.explorer.write . If you performed some cell filtering, see the next section. In [ ]: Copied! sopa . io . explorer . write ( explorer_path , sdata , mode = "+o" ) sopa.io.explorer.write(explorer_path, sdata, mode="+o") Visualize the new categories in the explorer ¶ To visualize these clusters, re-open the experiment.xenium file and select the new "leiden" cell group (under the "Cells" panel and in the "Cell groups" dropdown). See the examples above to see how it looks on the Xenium Explorer. Filtering cells ¶ Sometimes, you may want to filter some cells after running Sopa. If you want these cells to be also removed in the Xenium Explorer, you'll need to update the cell boundaries, cell counts, and cell observation files. To do that, you can re-run sopa.io.explorer.write , but provide mode="-it" to avoid creating again the images and transcript files - which are the most time-consuming files to create. In [ ]: Copied! # update all files except the images and transcript files sopa . io . explorer . write ( explorer_path , sdata , mode = "-it" ) # update all files except the images and transcript files sopa.io.explorer.write(explorer_path, sdata, mode="-it") Use the coordinates of a lasso selection in SpatialData ¶ On the Xenium Explorer, you can use the Lasso or Rectangular selection tools to select some regions of interest. Then, you'll be able to analyze back this region of interest using spatialdata . Selecting cells from a selection ¶ After making a selection, click on "Download Cell Stats as CSV", as below. It will create a file called "Selection_1_cells_stats.csv" . In [9]: Copied! import pandas as pd # write below the path to the file that you downloaded, e.g. "Selection_1_cells_stats.csv" df_selection = pd . read_csv ( "Selection_1_cells_stats.csv" , skiprows = 2 ) # we create a new column to annotate which cells were selected or not adata . obs [ "lasso" ] = "not-selected" adata . obs . loc [ df_selection [ "Cell ID" ] . values , "lasso" ] = "selected" import pandas as pd # write below the path to the file that you downloaded, e.g. "Selection_1_cells_stats.csv" df_selection = pd.read_csv("Selection_1_cells_stats.csv", skiprows=2) # we create a new column to annotate which cells were selected or not adata.obs["lasso"] = "not-selected" adata.obs.loc[df_selection["Cell ID"].values, "lasso"] = "selected" Now, sdata.table.obs["lasso"] denotes which cells have been selected by the lasso tool (either "selected" or "not-selected"). We can then use spatialdata_plot to display the cells that were selected by the lasso tool. Make sure to install spatialdata_plot &gt;= 0.1.0 , e.g. via pip install spatialdata_plot . Now, we can render the shapes (i.e., the spots) and colour them based on the "lasso" column that was saved in sdata.table.obs : In [10]: Copied! import spatialdata_plot import spatialdata_plot In [11]: Copied! sdata . pl . render_shapes ( color = "lasso" ) . pl . show ( "global" ) sdata.pl.render_shapes(color="lasso").pl.show("global") /Users/quentinblampey/mambaforge/envs/sopa/lib/python3.10/site-packages/spatialdata_plot/pl/basic.py:879: UserWarning: Converting copy of 'lasso' column to categorical dtype for categorical plotting. Consider converting before plotting. _render_shapes( /Users/quentinblampey/mambaforge/envs/sopa/lib/python3.10/site-packages/spatialdata_plot/pl/utils.py:782: FutureWarning: The default value of 'ignore' for the `na_action` parameter in pandas.Categorical.map is deprecated and will be changed to 'None' in a future version. Please set na_action to the desired value to avoid seeing this warning color_vector = color_source_vector.map(color_mapping) Cropping a Spa