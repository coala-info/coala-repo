---
name: bioconductor-cma
description: the package is available on all platforms; click for details.
homepage: https://bioconductor.org/packages/release/bioc/html/CMA.html
---

# bioconductor-cma

## Overview

Use the Bioconductor R package **CMA** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("CMA")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.