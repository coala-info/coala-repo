---
name: bioconductor-profileplyr
description: The bioconductor-profileplyr package visualizes and analyzes genomic signal profiles across multiple sets of genomic regions. Use when user asks to group genomic regions by signal intensity, sort ChIP-seq profiles, or export data for heatmap visualization.
homepage: https://bioconductor.org/packages/release/bioc/html/profileplyr.html
---


# bioconductor-profileplyr

## Overview

Use the Bioconductor R package **profileplyr** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("profileplyr")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.