---
name: bioconductor-remp
description: the package is available on all platforms; click for details.
homepage: https://bioconductor.org/packages/release/bioc/html/REMP.html
---

# bioconductor-remp

## Overview

Use the Bioconductor R package **REMP** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("REMP")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.