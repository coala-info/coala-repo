---
name: bioconductor-plgem
description: This R package implements the Power Law Global Error Model for identifying differentially expressed genes or proteins in high-throughput data. Use when user asks to identify differentially expressed genes, model signal-dependent noise in high-throughput datasets, or apply the Power Law Global Error Model.
homepage: https://bioconductor.org/packages/release/bioc/html/plgem.html
---


# bioconductor-plgem

## Overview

Use the Bioconductor R package **plgem** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("plgem")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.