---
name: bioconductor-scds
description: the package is available on all platforms; click for details.
homepage: https://bioconductor.org/packages/release/bioc/html/scds.html
---

# bioconductor-scds

## Overview

Use the Bioconductor R package **scds** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("scds")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.