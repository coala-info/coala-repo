# vrhyme CWL Generation Report

## vrhyme_vRhyme

### Tool Description
Binning Virus Genomes from Metagenomes

### Metadata
- **Docker Image**: quay.io/biocontainers/vrhyme:1.1.0--pyhdfd78af_1
- **Homepage**: https://github.com/AnantharamanLab/vRhyme
- **Package**: https://anaconda.org/channels/bioconda/packages/vrhyme/overview
- **Validation**: PASS

- **Conda**: https://anaconda.org/channels/bioconda/packages/vrhyme/overview
- **Total Downloads**: 3.9K
- **Last updated**: 2025-04-22
- **GitHub**: https://github.com/AnantharamanLab/vRhyme
- **Stars**: N/A
### Original Help Text
```text
[1m
#########################################################
#  vRhyme: Binning Virus Genomes from Metagenomes       #
#  Version 1.1.0 (March 2022)                           #
#  University of Wisconsin-Madison, Anantharaman Lab    #
#  Contact: Kristopher Kieft (kieft@wisc.edu)           #
#########################################################

 USAGE:
 
 1. required: specify scaffolds with -i
 2. required: pick a coverage input (-c, -b, -s, -r, -u, or -v)
 3. common: provide -o, -t, -p, -g and/or -l
 4. optional: modify run with other flags

 EXAMPLES: 

 minimum input example with bam files:
 vRhyme -i fasta -b bam_folder/*.bam

 minimum input example with a coverage file:
 vRhyme -i fasta -c coverage_file.tsv

 full BAM input example:
 vRhyme -i fasta -g genes -p proteins -b bam_folder/*.bam -t threads -o output_folder

 reads input example:
 vRhyme -i fasta -g genes -p proteins -r paired_reads_folder/*.fastq -t threads -o output_folder --method longest

 only use dereplicate function:
 vRhyme -i input_fasta -t threads -o output_folder --derep_only --method longest

___________________________________________________________________
[0m

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit

REQUIRED:
  -i                    input nucleotide scaffolds

PICK ONE COVERAGE INPUT:
  -c                    coverage table generated from vRhyme or from cov_table_convert.py
  -b [B ...]            BAM (.bam) sequence alignment files, sorted or unsorted.
  -s [S ...]            SAM (.sam) sequence alignment files.
  -r [R ...]            paired forward (_1.fastq) and reverse (_2.fastq) read files
  -u [U ...]            unpaired (.fastq) read files
  -v [V ...]            interleaved paired (.fastq) read files, '--aligner bowtie2' only

COMMONLY USED:
  -o                    output folder [vRhyme_results_<input_scaffolds_name>]
  -t                    number of parallel processes to run (equivalent to threads, see --speed) [5]
  -g                    input nucleotide genes in format name_#, skip to run Prodigal
  -p                    input amino acid proteins in format name_#, skip to run Prodigal
  -l                    minimum scaffold length to consider for binning [2000]

MODIFY OUTPUTS:
  --keep_circ           do not remove circular sequences before binning [off].
  --interest            file containing a list of scaffolds of interest to dereplicate/bin.
  --keep_sam            do not remove SAM files generated by vRhyme, reads input only [off].
  --keep_bam            do not remove unsorted/index BAM files generated by vRhyme, always keep sorted [off].
  --speed               allow extra CPU multithreading per process (may use more CPUs than '-t <int>' per process) [off].
  --verbose             write log file information to standard out while running [off].
  --prefix              prefix to append to binned scaffold names, use the '#' symbol where the bin number will be noted [vRhyme_#__]

READ ALIGNMENT:
  --aligner {bowtie2,bwa}
                        read alignment software to use [bowtie2]
  --read_id             minimum percent identity per aligned read for calculating coverage [0.97].
  --mask                mask coverage values <int> bases on each end of a scaffold [150]

BIN FILTERS:
  --outliers            remove outlier coverage values <val> standard deviations above average [4.0]
  --bin_size            minimum number of scaffolds per bin [2]
  --iter                number of binning iterations (presets) to run [20, range: 10-20]
  --red                 maximum number of redundant proteins per bin [50]
  --cov                 coverage threshold to consider scaffold as present in sample [0.80]
  --model {hybrid,NN,ET}
                        machine learning model(s) to use [hybrid]
  --max_gc              maximum gc distance for pre-filtering [0.20]
  --min_kmer            minimum k-mer distance for pre-filtering [0.60]
  --max_edges           maximum number of edges per node in network clustering [dynamic, 3-6]
  --penalty_w           penalty weight for Cohen's d distance calculations [0.20]
  --penalty_n           maximum number of penalties for Cohen's d distance calculations [10]
  --mems                refine bins with at least <int> members [4]

DEREPLICATION:
  --derep_only          only use dereplication function, skip binning [off].
  --method {composite,longest,none}
                        method of input scaffold dereplication, 'composite' compatible with reads input or --derep_only [none].
  -m                    minimum scaffold length to consider for dereplication [1000]
  --derep_id            minimum percent similarity for dereplication [composite=0.99, longest=0.97].
  --frac                minimum coverage in overlap percent for dereplication [composite=0.20, longest=0.70].
  --mash_k              equivalent to mash sketch -k [31].
  --mash_s              equivalent to mash sketch -s [1000].
  --sens_ends           free ends sensitivity to merging over regions of complexity; requires '--method composite' [0.10].
  --sens_overlap        alignment overlap sensitivity to merging over regions of complexity; requires '--method composite' [0.25].
  --nucmer_c            equivalent to nucmer -c [1000].
  --nucmer_b            equivalent to nucmer -b [1000].
  --nucmer_g            equivalent to nucmer -g [1000].
  --nuc_split           maximum fragmentations allowed per scaffold in nucmer alignment [3].
```


## vrhyme_test_vRhyme.py

### Tool Description
No inputs — do not generate CWL.

### Metadata
- **Docker Image**: quay.io/biocontainers/vrhyme:1.1.0--pyhdfd78af_1
- **Homepage**: https://github.com/AnantharamanLab/vRhyme
- **Package**: https://anaconda.org/channels/bioconda/packages/vrhyme/overview
- **Validation**: FAIL (generation failed)

### Generation Failed

No inputs — do not generate CWL.


### Validation Errors

- No inputs — do not generate CWL.



### Original Help Text
```text
This script is used to test the setup of vRhyme dependencies. 
        There are no input arguments, simply run the script. 
        All test results are printed to the standard out.
        "Success" statements mean the dependency should be good.
```

