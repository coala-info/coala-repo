---
name: bioconductor-mbkmeans
description: The bioconductor-mbkmeans package implements the mini-batch k-means algorithm for efficient clustering of large-scale genomic datasets. Use when user asks to cluster large single-cell RNA-seq datasets, perform mini-batch k-means clustering, or scale k-means to massive data matrices.
homepage: https://bioconductor.org/packages/release/bioc/html/mbkmeans.html
---


# bioconductor-mbkmeans

## Overview

Use the Bioconductor R package **mbkmeans** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("mbkmeans")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.