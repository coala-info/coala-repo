---
name: bioconductor-sictools
description: SICtools identifies and compares single nucleotide variants and indels between two BAM files using statistical tests. Use when user asks to compare variants between paired samples, identify SNVs and indels from BAM files, or perform statistical comparisons of genomic variants.
homepage: https://bioconductor.org/packages/release/bioc/html/SICtools.html
---


# bioconductor-sictools

## Overview

Use the Bioconductor R package **SICtools** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("SICtools")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.