---
name: bioconductor-randrotation
description: The randRotation package implements random rotation methods for linear models to perform hypothesis testing in high-dimensional genomic data. Use when user asks to perform gene set testing, rotate data matrices, or conduct inference for linear models with dependent observations.
homepage: https://bioconductor.org/packages/release/bioc/html/randRotation.html
---


# bioconductor-randrotation

## Overview

Use the Bioconductor R package **randRotation** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("randRotation")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.