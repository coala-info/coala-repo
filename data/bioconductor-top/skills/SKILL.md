---
name: bioconductor-top
description: The TOP package predicts transcription factor binding sites and occupancy using chromatin accessibility data. Use when user asks to predict transcription factor binding, estimate occupancy profiles, or analyze DNase-seq and ATAC-seq data across different cell types.
homepage: https://bioconductor.org/packages/release/bioc/html/TOP.html
---


# bioconductor-top

## Overview

Use the Bioconductor R package **TOP** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("TOP")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.