---
name: bioconductor-snifter
description: The bioconductor-snifter package provides an R interface for fast t-SNE dimensionality reduction using the openTSNE implementation. Use when user asks to perform t-SNE dimensionality reduction, run openTSNE from R, or embed high-dimensional data into a lower-dimensional space.
homepage: https://bioconductor.org/packages/release/bioc/html/snifter.html
---


# bioconductor-snifter

## Overview

Use the Bioconductor R package **snifter** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("snifter")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.