---
name: bioconductor-mdp
description: This tool quantifies the molecular degree of perturbation in omics data by comparing samples to a reference group. Use when user asks to calculate molecular degree of perturbation, assess sample heterogeneity, or identify perturbed samples in high-throughput datasets.
homepage: https://bioconductor.org/packages/release/bioc/html/mdp.html
---


# bioconductor-mdp

## Overview

Use the Bioconductor R package **mdp** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("mdp")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.