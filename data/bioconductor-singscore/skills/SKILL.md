---
name: bioconductor-singscore
description: Singscore implements a rank-based method for calculating gene set enrichment scores at the single-sample level. Use when user asks to score individual samples against gene signatures, perform single-sample gene set analysis, or rank genes within a transcriptomic profile.
homepage: https://bioconductor.org/packages/release/bioc/html/singscore.html
---


# bioconductor-singscore

## Overview

Use the Bioconductor R package **singscore** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("singscore")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.