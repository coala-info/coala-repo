---
name: bioconductor-scbubbletree
description: the package is available on all platforms; click for details.
homepage: https://bioconductor.org/packages/release/bioc/html/scBubbletree.html
---

# bioconductor-scbubbletree

## Overview

Use the Bioconductor R package **scBubbletree** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("scBubbletree")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.