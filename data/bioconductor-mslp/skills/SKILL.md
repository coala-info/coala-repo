---
name: bioconductor-mslp
description: the package is available on all platforms; click for details.
homepage: https://bioconductor.org/packages/release/bioc/html/mslp.html
---

# bioconductor-mslp

## Overview

Use the Bioconductor R package **mslp** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("mslp")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.