# mea CWL Generation Report

## mea

### Tool Description
The tool accepts dot plot files as generated by RNAfold -p.
If a reference structure is provided, the predicted (or given) structure is compared to this reference. The tool accepts dot-bracket structures with pseudoknots (supporting bracket pairs (),{},[],<>,Aa,Bb,...)
The result of structure comparison is reported as a line of numbers

<TP> <FP> <FN> <TN> <SENS> <PPV> <F1> <MCC>

where 
  TP   = # true positives
  FP   = # false positives
  FN   = # false negatives
  TN   = # true negatives
  SENS = TP/(TP+FN) 'Sensitivity'
  PPV  = TP/(TP+FP) 'Positive Predictive Value'
  F1   = PPV*SENS / (PPV+SENS), if PPV+SENS!=0; 0, otherwise   'F1-score'
  MCC  = (TP*TN - FP*FN) / sqrt( (TP+FP)*(TP+FN)*(TN+FP)*(TN+FN) )
                                        'Mathews correlation coefficient'

Special rules for prediction evaluation:
  * Slide rule: tolerate shift of one base pair end by one base.
    This rule directly affects the number of true positives.
  * Conflict rule: predicted base pairs are false only if they conflict with
    the reference; two base pair conflict if and only if they share one end
    This rule directly affects the number of false positives.

### Metadata
- **Docker Image**: quay.io/biocontainers/mea:0.6.4--h9948957_10
- **Homepage**: http://www.bioinf.uni-leipzig.de/Software/mea/
- **Package**: https://anaconda.org/channels/bioconda/packages/mea/overview
- **Validation**: PASS

- **Conda**: https://anaconda.org/channels/bioconda/packages/mea/overview
- **Total Downloads**: 8.3K
- **Last updated**: 2025-08-14
- **GitHub**: N/A
- **Stars**: N/A
### Original Help Text
```text
MEA 0.6.4

Usage: mea[options] dotplot_filename

  -h, --help              Print help and exit
  -V, --version           Print version and exit
  -a, --alpha=DOUBLE      slope of base pair distance penalty
                            (default=`0.012')
  -b, --beta=DOUBLE       turning point of base pair distance penalty
                            (default=`315')
  -g, --gamma=DOUBLE      base pair weight factor  (default=`0.5')
  -d, --delta=DOUBLE      minimum penalty factor for base pairs
                            (default=`0.003')
  -p, --plot-penalty=INT  print penalty values between 4 and given value; don't
                            fold
  -r, --reference=STRING  Reference structure (dot-bracket)
  -s, --structure=STRING  Structure (dot-bracket); if given, compare to
                            reference only
  -S, --no-slide-rule     Use slide rule [default: use slide rule]
  -C, --no-conflict-rule  Use conflict rule [default: use conflict rule]
  -v, --verbose           Turn on verbose output

The tool accepts dot plot files as generated by RNAfold -p.

If a reference structure is provided, the predicted (or given)
structure is compared to this reference. The tool accepts dot-bracket
structures with pseudoknots (supporting bracket pairs (),{},[],<>,Aa,Bb,...)

The result of structure comparison is reported as a line of numbers

<TP> <FP> <FN> <TN> <SENS> <PPV> <F1> <MCC>

where 
  TP   = # true positives
  FP   = # false positives
  FN   = # false negatives
  TN   = # true negatives
  SENS = TP/(TP+FN) 'Sensitivity'
  PPV  = TP/(TP+FP) 'Positive Predictive Value'
  F1   = PPV*SENS / (PPV+SENS), if PPV+SENS!=0; 0, otherwise   'F1-score'
  MCC  = (TP*TN - FP*FN) / sqrt( (TP+FP)*(TP+FN)*(TN+FP)*(TN+FN) )
                                        'Mathews correlation coefficient'

Special rules for prediction evaluation:
  * Slide rule: tolerate shift of one base pair end by one base.
    This rule directly affects the number of true positives.
  * Conflict rule: predicted base pairs are false only if they conflict with
    the reference; two base pair conflict if and only if they share one end
    This rule directly affects the number of false positives.
```

