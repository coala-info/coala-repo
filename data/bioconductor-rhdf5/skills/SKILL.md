---
name: bioconductor-rhdf5
description: The rhdf5 package provides an interface for reading and writing HDF5 files within the R environment. Use when user asks to read HDF5 files, write data to HDF5 format, or manage large-scale hierarchical datasets in R.
homepage: https://bioconductor.org/packages/release/bioc/html/rhdf5.html
---


# bioconductor-rhdf5

## Overview

Use the Bioconductor R package **rhdf5** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("rhdf5")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.