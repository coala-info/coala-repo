---
name: bioconductor-treg
description: The bioconductor-treg package identifies candidate genes for estimating total RNA content in single-cell or spatial transcriptomics data. Use when user asks to identify total RNA expression genes, select marker genes for spatial transcriptomics, or estimate RNA abundance across different cell types.
homepage: https://bioconductor.org/packages/release/bioc/html/TREG.html
---


# bioconductor-treg

## Overview

Use the Bioconductor R package **TREG** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("TREG")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.