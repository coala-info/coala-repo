---
name: bioconductor-swfdr
description: This package estimates the science-wise false discovery rate and performs multiple testing adjustment using covariate information. Use when user asks to estimate the science-wise false discovery rate, perform FDR control with covariates, or adjust p-values using auxiliary information.
homepage: https://bioconductor.org/packages/release/bioc/html/swfdr.html
---


# bioconductor-swfdr

## Overview

Use the Bioconductor R package **swfdr** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("swfdr")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.