---
name: bioconductor-mantelcorr
description: This R package computes Mantel cluster correlations for gene expression data to identify and evaluate clusters. Use when user asks to compute Mantel cluster correlations, evaluate the significance of gene clusters, or perform cluster analysis on expression profiles.
homepage: https://bioconductor.org/packages/release/bioc/html/MantelCorr.html
---


# bioconductor-mantelcorr

## Overview

Use the Bioconductor R package **MantelCorr** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("MantelCorr")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.