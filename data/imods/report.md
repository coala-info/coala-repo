# imods CWL Generation Report

## imods_imove

### Tool Description
Internal coordinates MOVEment tool.

### Metadata
- **Docker Image**: quay.io/biocontainers/imods:1.0.4--h9ee0642_3
- **Homepage**: https://chaconlab.org/multiscale-simulations/imod
- **Package**: https://anaconda.org/channels/bioconda/packages/imods/overview
- **Validation**: PASS

- **Conda**: https://anaconda.org/channels/bioconda/packages/imods/overview
- **Total Downloads**: 3.0K
- **Last updated**: 2025-04-22
- **GitHub**: N/A
- **Stars**: N/A
### Original Help Text
```text
imove>
imove> Welcome to the Internal Coordinates Motion Tool v1.14
imove>

USAGE: 

   imove  <in_pdb> <ptraj> <out_pdb> <int> [-m <int>] [-x] [-c <int>] [-a
          <float>] [-T <double>] [-f <string>] [--cart] [--model_out <int>]
          [--linear] [--mov <int>] [-p <string>] [--initial <int>] [--final
          <int>] [--nthreads <int>] [--nomodel] [--chi_out] [--fixIC
          <string>] [--energy <string>] [--] [-v] [-h]


Where: 

   <in_pdb>
      PDB input file. (required)  

   <ptraj>
      Normal modes input file (.evec). (required)  

   <out_pdb>
      Output Multi-PDB file. (required)  

   <int>
      Mode number to be moved (1,2,...,size). If --proj option enabled,
     then it sets the number of components to be used in ED-trajectory
     filtration. (required)  

   -m <int>,  --model <int>
      Input modes Coarse-Graining model: 0=CA, 1=C5, 2=Heavy-Atom, 3=NCAC,
     4=CA-only (default=2). 

   -x,  --chi
      Considers first CHI dihedral angle (default=disabled). 

   -c <int>,  --frames <int>
      Number of conformations generated (default=11). It should be an odd
     number! 

   -a <float>,  --amp <float>
      Amplitude linear factor to scale motion (default=2). 

   -T <double>,  --temperature <double>
      Temperature [K] (default=300). 

   -f <string>,  --fixFile <string>
      Input ASCII file defining the ICs that were fixed during NMA
     (default=disabled). If modes were computed removing arbitrary ICs, the
     user must introduce here the file generated by iMode's --save_fixfile
     option. 

   --cart
      Mandatory if Cartesian modes are supplied, otherwise moving in
     Internal Coordinates (default=disabled). 

   --model_out <int>
      Output Coarse-Graining model: 0=CA, 1=C5, 2=Heavy-Atom. The default
     output model will be that selected with the -m option. 

   --linear
      Enables linear motion instead of sinusoidal bounce
     (default=disabled). 

   --mov <int>
      Motion Type (default=2): 0=K-matrix, 1=V/W-arrays, 2=Simple-Rotations
     , 3=Linear (if Cartesian modes). 

   -p <string>,  --proj <string>
      ED-trajectory projection file for filtration. The projection file
     with the components for each frame should be provided here. 

   --initial <int>
      Initial frame index as defined in proj file (default= first) 

   --final <int>
      Final frame index as defined in proj file (default= last) (only
     active if --proj option is enabled) 

   --nthreads <int>
      Number of threads for parallel processing (only used if --mov 1) 

   --nomodel
      Disables PDB model building. Warning: introduced PDB model must match
     the CG selected with the -m option (default=disabled). 

   --chi_out
      Considers first CHI dihedral angle in output models
     (default=disabled) (DEVELOPER's). 

   --fixIC <string>
      Plain-text file defining the fixed Internal Coordinates. Each line
     will contain the index (0,1,...) of the ICs to be removed
     (DEVELOPER's). 

   --energy <string>
      Enables harmonic energy calculations from the provided force
     constants file (Kfile) (DEVELOPER's). 

   --,  --ignore_rest
      Ignores the rest of the labeled arguments following this flag. 

   -v,  --version
      Displays version information and exits. 

   -h,  --help
      Displays usage information and exits. 


   Internal coordinates MOVEment tool.
```


## imods_imodview

### Tool Description
Cartesian Normal Modes and Springs Visualization tool

### Metadata
- **Docker Image**: quay.io/biocontainers/imods:1.0.4--h9ee0642_3
- **Homepage**: https://chaconlab.org/multiscale-simulations/imod
- **Package**: https://anaconda.org/channels/bioconda/packages/imods/overview
- **Validation**: PASS

### Original Help Text
```text
imodview>
imodview> Welcome to the Normal Modes and Springs visualization Tool  v1.08
imodview>

USAGE: 

   imodview  <pdb> <ptraj/Kfile> <vmd> [-n <int>] [--color <string>] [--op
             <int>] [--max <float>] [--thick <float>] [--level <int>]
             [--nresidues <int>] [--pthr <float>] [--kthr <float>] [--kthr2
             <float>] [-o <string>] [--nclusters <int>] [-f <string>]
             [--adj_cutoff <float>] [--extend_size <int>] [--extend_cutoff
             <float>] [--save_pdb_clusters] [--format <int>]
             [--save_affine] [--save_axis] [--save_paths] [--save_valid]
             [--save_sphere] [--server] [--] [-v] [-h]


Where: 

   <pdb>
      PDB input file. Warning: It must match exactly that used in NMA.
     (required)  

   <ptraj/Kfile>
      Input eigenvectors (ptraj) or force constants (Kfile) file. Warning:
     Only Cartesian modes allowed. (required)  

   <vmd>
      Ouput VMD file. (required)  

   -n <int>,  --nev <int>
      Normal Mode index (1,2,...,size) (default=1). 

   --color <string>
      Set color. All VMD colors available (default=black). 

   --op <int>
      Sets the operation method, 1=Arrows, 2=Springs, 3=Bryden's
     (default=1). 

   --max <float>
      Maximum arrow length [A] (default=10). 

   --thick <float>
      Arrow/spring thickness factor (default=0.05). 

   --level <int>
      Sets the averaging level to compute arrows, 0=atoms, 1=residues,
     2=segments, 3=chains (default=0). 

   --nresidues <int>
      Sets the number of residues for averaging (only used by "--level 1"
     option (default=1). 

   --pthr <float>
      Minimum percentual amplitude (from maximum) to show arrows (default=0
     , all arrows). 

   --kthr <float>
      Only those springs with force constants above this threshold will be
     shown (default=disabled). 

   --kthr2 <float>
      Only those springs with force constants below this threshold will be
     shown (default=disabled). 

   -o <string>,  --name <string>
      Output files basename (default=disabled). 

   --nclusters <int>
      Number of requested clusters. (Default=10) 

   -f <string>,  --fixFile <string>
      Input ASCII file defining the ICs that were fixed during NMA
     (default=disabled). If modes were computed removing arbitrary ICs, the
     user must introduce here the file generated by iMode's --save_fixfile
     option. 

   --adj_cutoff <float>
      Adjacency cutoff (in Angstroms). (Default=6A) 

   --extend_size <int>
      Number of atoms threshold to consider cluster extension. Only
     clusters with less or equal number atoms will be extended. (Default=5)

   --extend_cutoff <float>
      Distance (in Angstroms) to extend small clusters. (Default=10A) 

   --save_pdb_clusters
      Save pdbs with cluster information stored in the B-factors.
     (default=disabled) 

   --format <int>
      Arrows output format: 1=VMD, 2=JMol(PMESH). Only for arrows.
     (Default=JMol) 

   --save_affine
      Plot the arrows obtained from the calculated affine model (in
     arbitrary units). (default=disabled) 

   --save_axis
      Plot the cluster axis or rotation assuming rigid body motion.
     (default=disabled) 

   --save_paths
      Plot the full paths obtained from every valid point.
     (default=disabled) 

   --save_valid
      Plot the valid points, i.e. those not colliding with any atom.
     (default=disabled) 

   --save_sphere
      Plot the points homogeneously distributed over a sphere centered on
     each cluster. (default=disabled) 

   --server
      Enable "server mode" to maximize the automatic selection of
     parameters. (default=disabled) 

   --,  --ignore_rest
      Ignores the rest of the labeled arguments following this flag. 

   -v,  --version
      Displays version information and exits. 

   -h,  --help
      Displays usage information and exits. 


   Cartesian Normal Modes and Springs Visualization tool
```


## imods_imc

### Tool Description
Monte-Carlo IC-NMA based program v1.11

### Metadata
- **Docker Image**: quay.io/biocontainers/imods:1.0.4--h9ee0642_3
- **Homepage**: https://chaconlab.org/multiscale-simulations/imod
- **Package**: https://anaconda.org/channels/bioconda/packages/imods/overview
- **Validation**: PASS

### Original Help Text
```text
imc>
imc> Monte-Carlo IC-NMA based program v1.11
imc>

USAGE: 

   imc  <pdb> <ptraj> [-m <int>] [-o <string>] [-f <string>] [-p <float>]
        [-g <float>] [-n <int>] [-c <int>] [-E <double>] [-i <int>] [-x]
        [-T <double>] [-a <float>] [--perc <float>] [--thr <float>] [--Rmsd
        <float>] [--otraj <int>] [--cart] [--seed <unsigned int>]
        [--include_first] [--var] [--unweight] [--optf <float>] [--rfact
        <double>] [--mov <int>] [--fixIC <string>] [--verb] [--] [-v] [-h]


Where: 

   <pdb>
      PDB input file.  (required)  

   <ptraj>
      Normal modes input file (.evec), either from NMA or PCA. (required)  

   -m <int>,  --model <int>
      Input Coarse-Graining model: 0=CA, 1=C5, 2=Heavy-Atom (default=2). 

   -o <string>,  --name <string>
      Output files basename (default=imc). 

   -f <string>,  --fixFile <string>
      Input ASCII file defining the ICs that were fixed during NMA
     (default=disabled). If modes were computed removing arbitrary ICs, the
     user must introduce here the file generated by iMode's --save_fixfile
     option. 

   -p <float>,  --optRmsd <float>
      Finds the optimal energy/stiffness scaling factor to obtain the
     desired average RMSD (A) from the initial model (default=disabled). 

   -g <float>,  --Rg <float>
      Filter models by target radius of gyration (default=disabled). 

   -n <int>,  --nevs <int>
      Number of eigenvectors to be employed, either number [1,N] <integer>,
     or ratio from maximum available [0,1) <float> (default=5). 

   -c <int>,  --frames <int>
      Number of output conformations (default= 100). 

   -E <double>,  --efact <double>
      Energy/Stiffness scaling factor (mode energy will be multiplied by
     this value) (default=1.0). 

   -i <int>,  --iter <int>
      Number of MC iterations per output structure (default=1000). 

   -x,  --chi
      Considers first CHI dihedral angle (default=disabled). 

   -T <double>,  --temperature <double>
      Temperature [K] (default=300). 

   -a <float>,  --amp <float>
      Amplitude linear factor to scale motion (default=1). 

   --perc <float>
      Enable filtering by tolerance percentage (default=1, i.e. 1%). 

   --thr <float>
      Enable filtering by absolute tolerance (default=disabled). 

   --Rmsd <float>
      Filter models by target RMSD (default=disabled). 

   --otraj <int>
      Output trajectory format: 0-Normal-Mode, 1-Multi-PDB, 2-AMBER
     (default=1). 

   --cart
      Mandatory if Cartesian modes are supplied, otherwise moving in
     Internal Coordinates (default=disabled). 

   --seed <unsigned int>
      Set the random number generator SEED (Mersenne Twister)
     (default=random-seed from /dev/urandom). 

   --include_first
      Includes input model as first frame in the Multi-PDB trajectory
     (default=disabled). 

   --var
      Input eigenvalues will be considered as variance (pca), otherwise the
     will be force constants (nma) (default=false). 

   --unweight
      Un-mass-weights the input vectors (default=false). The Mass-weighted
     modes (wcart) will be converted into Cartesian coordiantes (cart)
     (DEVELOPER's). 

   --optf <float>
      Factor to scale the optimal energy/stiffness factor
     (default=disabled) (DEVELOPER's). 

   --rfact <double>
      Agressivity factor (default=7.778) (DEVELOPER's). 

   --mov <int>
      Motion Type (default=2): 0=K-matrix, 1=V/W-arrays, 2=Simple-Rotations
     , 3=Linear (if Cartesian modes) (DEVELOPER's). 

   --fixIC <string>
      Plain-text file defining the fixed Internal Coordinates. Each line
     will contain the index (0,1,...) of the ICs to be removed
     (DEVELOPER's). 

   --verb
      Enables verbose. 

   --,  --ignore_rest
      Ignores the rest of the labeled arguments following this flag. 

   -v,  --version
      Displays version information and exits. 

   -h,  --help
      Displays usage information and exits. 


   Monte-Carlo program based on normal modes.
```


## Metadata
- **Skill**: generated
