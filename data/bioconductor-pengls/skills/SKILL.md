---
name: bioconductor-pengls
description: This tool fits penalized generalized least squares models for high-dimensional data with known correlation structures. Use when user asks to fit penalized generalized least squares models, perform variable selection on correlated data, or handle high-dimensional regression with phylogenetic or spatial covariance.
homepage: https://bioconductor.org/packages/release/bioc/html/pengls.html
---


# bioconductor-pengls

## Overview

Use the Bioconductor R package **pengls** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("pengls")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.