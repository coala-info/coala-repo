---
name: bioconductor-scmerge
description: scMerge performs batch effect correction and normalization for single-cell RNA-sequencing data across multiple batches or studies. Use when user asks to merge single-cell datasets, remove batch effects, or integrate scRNA-seq data from different platforms.
homepage: https://bioconductor.org/packages/release/bioc/html/scMerge.html
---


# bioconductor-scmerge

## Overview

Use the Bioconductor R package **scMerge** for: the package is available on all platforms; click for details.

## Installation

```r
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("scMerge")
```

## Reference documentation

See files in `references/` for vignettes and tutorials.